# Core Streamlit and Web App
streamlit
Pillow

# AI/ML Libraries
torch
transformers>=4.49.0
accelerate
peft
trl
tokenizers
sentencepiece
huggingface_hub
pandas

# For Live Explanation API
# The server itself, needs to be installed in the environment where you run the API
llama-factory 
# Client library to connect to the local OpenAI-compatible API
openai